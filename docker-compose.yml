services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./database:/app/database
    env_file:
      - .env
    restart: unless-stopped
    environment:
      - FLASK_ENV=production
